# Voice Input Desktop 详细开发与验收计划

版本：v0.1 规划稿  
目标平台：macOS（Apple Silicon / Intel）与 Windows x64  
技术栈：Electron + React + TypeScript + shadcn/ui + sherpa-onnx + CDP

## 1. 产品目标

创建一个常驻桌面的语音输入工具。用户可以在进行其他键盘和鼠标操作时持续说话；应用在本地完成 VAD、语音识别和声纹验证，只把验证为用户本人的语句写入 ChatGPT 桌面端输入框。

首版默认遵循以下安全行为：

- 识别内容默认只写入 ChatGPT 输入框，不自动发送。
- 每个 VAD 语句完成并通过声纹验证后才允许写入。
- 未通过声纹验证的音频和识别结果直接丢弃。
- CDP 仅连接 `127.0.0.1`，不接受局域网调试端口。
- 音频、声纹和识别默认完全在本地处理。

## 2. 首版范围

### 包含

- 首次引导：麦克风、模型、声纹、ChatGPT 连接。
- 语音输入主页：服务状态、实时预览、开始/暂停。
- 声纹页面：录制、登记、删除、阈值校准。
- 目标应用页面：检测 ChatGPT、连接状态、输入行为。
- 设置页面：开机启动、悬浮球、语言、停顿、标点、快捷键。
- 悬浮球：待机、监听、验证成功、验证失败、异常状态。
- 本地流式 ASR、VAD、声纹验证、标点处理。
- CDP 写入 ChatGPT 输入框与可选发送。
- macOS 与 Windows 安装包。
- 本地诊断日志与可导出的脱敏诊断报告。

### 首版不包含

- 云端语音识别。
- 多人会议的说话人分离。
- 银行级防录音回放和深度伪造检测。
- 除 ChatGPT 外的任意应用自动适配。
- Windows ARM64 正式发布包。
- 自动操控 ChatGPT 会话列表或读取历史对话。

## 3. 技术架构

```text
Electron 主进程
├── 窗口、托盘、快捷键、权限、开机启动
├── 配置与安全存储
├── 语音 sidecar 生命周期管理
└── ChatGPT CDP 适配器

React 渲染进程
├── shadcn/ui 页面
├── 悬浮球与展开面板
├── 音频设备设置
└── 状态、字幕、错误提示

AudioWorklet
└── 麦克风 PCM → 16 kHz 单声道音频帧

sherpa-onnx 独立 sidecar
├── VAD
├── 流式 ASR
├── Speaker Embedding / Verification
└── 标点恢复

CDP Adapter
└── 发现 ChatGPT 页面 → 定位输入框 → 写入/发送 → 断线重连
```

### 为什么使用独立 sidecar

生产版本不把 `sherpa-onnx-node` 直接加载到 Electron 进程。语音能力使用 sherpa-onnx C/C++ API 构建独立可执行程序，再由 Electron 启动和监管。这样可以规避 Electron Node ABI、asar、macOS 动态库路径和 Windows DLL 查找问题。

通信协议采用本机 WebSocket：

- 仅绑定 `127.0.0.1`。
- 启动时使用随机可用端口和一次性 token。
- 音频使用二进制帧，控制与结果使用版本化 JSON 消息。
- sidecar 退出时连接自动失效。

## 4. 必须先通过的技术预研关卡

在继续完整 UI 和业务开发前，先做一个无完整界面的垂直 PoC。以下四项必须全部通过。

### Spike A：本地实时识别

实现：麦克风音频经过 VAD 后进入流式中文/中英 ASR，终端输出 partial 与 final。

验证：

1. 连续朗读 20 句中文、10 句中英混合文本。
2. 每句话都产生 `speech_start`、若干 `partial`、`speech_end`、`final`。
3. 连续运行 30 分钟无崩溃、无持续内存增长。
4. 普通办公机器上最终结果延迟目标不超过句尾后 1.5 秒。

关卡：macOS Apple Silicon 与 Windows x64 都能独立跑通后通过。

### Spike B：声纹门控

实现：登记用户的多个声纹样本，对每个 VAD 语句进行本人/非本人判断。

验证：

1. 用户本人录制 30 句，统计通过率。
2. 至少两名其他人各录制 20 句，统计误通过率。
3. 测试近讲、远讲、轻声、正常语速和背景音乐。
4. 输出每次相似度，确定默认阈值而不是写死经验值。

关卡：安静环境中本人通过率 ≥ 95%，其他人误通过率 ≤ 5%；未达到则更换模型、增加登记样本或调整分段策略。

### Spike C：CDP 无焦点写入

实现：连接已开启调试端口的 ChatGPT 桌面端，向当前输入框写入指定字符串。

验证：

1. 焦点保持在另一个应用中，同时向 ChatGPT 写入文字。
2. 正确处理中文、英文、换行、emoji 和已有草稿。
3. 连续更新 partial 结果 100 次，不重复、不丢字。
4. ChatGPT 刷新、切换页面或暂时关闭后可以自动重连。
5. ChatGPT DOM 不匹配时停止写入并给出明确错误，不能误点其他元素。

关卡：macOS 与 Windows 均能完成 100 次连续写入，并且不抢占当前前台应用焦点。

### Spike D：最小跨平台打包

实现：把 Electron、sidecar 和一个最小模型打成安装包。

验证：

1. 在没有开发环境的新系统用户账户安装。
2. 启动、授权麦克风、运行一次识别、退出、再次启动。
3. macOS 检查签名、notarization 和麦克风权限文案。
4. Windows 检查 DLL、SmartScreen 签名准备和卸载残留。

关卡：两端干净环境都能完成一次端到端识别后通过。

## 5. 分阶段开发计划

## 阶段 0：需求冻结与测试基线

交付：

- 确认首版只支持 ChatGPT。
- 确认默认“写入但不发送”。
- 确认中英双语为首发语言。
- 建立标准测试音频：本人、非本人、静音、噪声、中英混说。
- 记录两台基准设备：一台 Apple Silicon Mac、一台 Windows x64。
- 定义消息协议与功能编号 F01–F30。

验收：需求清单和测试素材都有版本记录，后续每个缺陷可以复现。

## 阶段 1：四项高风险 PoC

完成 Spike A–D。PoC 可以使用临时界面，但必须形成自动脚本、测试数据和结果报告。

验收：四项关卡全部通过；任何一项失败都暂停完整产品开发，先修改技术路线。

## 阶段 2：工程骨架与 UI 页面

实现：

- Electron main / preload / renderer 分层。
- React + TypeScript + shadcn/ui。
- IPC 数据通过统一 schema 校验。
- 首次引导、语音输入、声纹、目标应用、设置五个页面。
- 单独的透明悬浮窗口和展开 Popover。
- 托盘菜单、单实例运行和窗口恢复。
- 亮色与暗色主题；默认采用当前 Notion 风格亮色。

验证：

- 组件测试覆盖页面主要状态。
- Playwright 逐页截图对比，防止布局回归。
- 320px 窄窗口与标准桌面尺寸均无溢出。
- 键盘可完成所有设置操作，焦点顺序正确。
- renderer 中不能直接使用 Node API。

验收：原型中的全部页面和悬浮状态都可交互，暂用 mock 数据即可。

## 阶段 3：麦克风与模型管理

实现：

- 麦克风授权、拒绝、重新授权说明。
- 音频设备枚举、选择、拔插重连。
- AudioWorklet 采集、重采样为 16 kHz 单声道。
- 音量表、削波检测、过低音量提示。
- 模型清单、下载、断点续传、SHA-256 校验、删除与空间检查。
- sidecar 启动、健康检查、异常退出重启。

验证：

- 单元测试覆盖采样率转换、帧长度和设置持久化。
- 使用虚拟音频夹具验证 PCM 输出内容。
- 下载中断后恢复，损坏文件必须被校验拒绝。
- 麦克风拔掉后 UI 在 2 秒内显示异常；重新插入后可恢复。
- sidecar 人工结束后最多自动重启 3 次，不能无限重启。

验收：在两端连续采集 60 分钟无音频队列堆积，模型管理全部跑通。

## 阶段 4：VAD、ASR 与文本流水线

实现：

- VAD speech start / end。
- 语句前置缓存，避免第一个字被截断。
- 流式 partial 与稳定 final。
- endpoint 停顿时间可配置。
- 在线或句尾标点恢复。
- `committedText + partialText` 草稿模型，避免重复追加。
- 识别取消、暂停、恢复和超长语句保护。

验证：

- 静音 10 分钟不能产生虚假语句。
- 每条标准音频的开头和结尾不能被截断。
- partial 多次修正后 final 只能出现一次。
- 快速连续说两句话时必须分成两段且顺序一致。
- 暂停后音频不能继续进入识别器。
- 30 分钟持续口述无重复段落、无顺序错乱。

验收：标准音频集全部产生确定的事件序列，异常输入不会使服务崩溃。

## 阶段 5：声纹登记与实时验证

实现：

- 引导录制 5–10 个声纹样本。
- 录制质量检查：时长、音量、静音比例、削波。
- embedding 提取、聚合、加密本地存储。
- 每个完整 VAD 语句提取 embedding 并验证。
- 阈值校准界面和恢复默认值。
- 通过、拒绝、音频太短、无法判断四种结果。
- 删除声纹后立即禁用“仅识别我的声音”。

验证：

- 单元测试覆盖阈值边界、无模板、损坏模板。
- 本人/非本人测试集输出混淆矩阵。
- 验证失败的语句绝不能进入 CDP 输出队列。
- 重启应用后声纹仍可使用；复制裸文件不能直接读取 embedding。
- 少于最低有效时长的音频显示“无法判断”，不能误判通过。

验收：达到 Spike B 的指标，并完成一次“本人通过、他人拒绝”的现场演示。

## 阶段 6：ChatGPT CDP 适配器

实现：

- 调试端口配置和安全校验。
- 获取 targets，识别 ChatGPT 会话页面。
- 连接、心跳、断线指数退避重连。
- Composer 适配器：读取草稿、设置草稿、追加、换行、发送。
- 输入框结构变化探测。
- 写入序列号和幂等处理，避免重试造成重复文本。
- 状态：未启动、连接中、已连接、页面不匹配、版本不兼容。

验证：

- 使用本地假的 CDP 页面做自动化契约测试。
- 使用真实 ChatGPT 做人工兼容性测试。
- 写入 1,000 个字符的中英混合文本保持完全一致。
- 已有草稿按设置选择保留或替换。
- 相同 message id 重试三次，输入框只能出现一次。
- 目标关闭、刷新、重新打开后恢复连接。
- 非本机地址和通配地址配置必须被拒绝。

验收：通过 Spike C 全部测试，并保存目标 ChatGPT 版本和验证日期。

## 阶段 7：端到端语音输入编排

实现事件状态机：

```text
IDLE → LISTENING → SPEECH_DETECTED → VERIFYING
                                      ├─ PASSED → COMMITTING → LISTENING
                                      ├─ REJECTED → DISCARDING → LISTENING
                                      └─ ERROR → RECOVERING / PAUSED
```

实现：

- 悬浮球与真实服务状态绑定。
- partial 只显示在本应用，默认不写入 ChatGPT。
- final 且声纹通过后加入输出队列。
- ChatGPT 暂时断线时保留待写入文本并提示用户。
- 用户暂停、清空、撤销上一句。
- 可选语音命令“发送”，默认关闭。
- 全局快捷键开始/暂停。

验证：

- 状态机所有转移都有自动测试。
- 乱序、重复、过期事件必须被忽略。
- 他人说话时 ChatGPT 输入框保持不变。
- 用户连续说 20 句，写入顺序与原始顺序完全一致。
- 网络不可用不影响本地识别。
- ChatGPT 断线后恢复，积压内容不会重复。
- 快捷键与鼠标操作不抢占用户当前窗口。

验收：完成一条真实端到端路径：启动 → 本人讲话 → VAD → ASR → 声纹通过 → ChatGPT 写入；以及一条拒绝路径。

## 阶段 8：系统集成与发布包

实现：

- macOS arm64/x64 构建、签名、notarization、DMG。
- Windows x64 构建、代码签名准备、安装器。
- sidecar 与模型资源的 unpack / 路径处理。
- 麦克风权限说明。
- 开机启动、托盘、退出清理。
- 自动更新先只实现检查与提示；静默更新不进入首版。

验证：

- 干净机器安装矩阵。
- 路径包含中文、空格时仍能启动。
- 普通用户权限安装和运行。
- 升级安装保留配置与声纹；卸载按用户选择保留或删除数据。
- 应用退出后无残留 sidecar 进程、端口和麦克风占用。
- macOS Gatekeeper 与 Windows Defender 基础扫描通过。

验收：两端安装包各在至少两台机器完成安装、识别、写入、退出、重启全流程。

## 阶段 9：稳定性、性能与隐私验收

实现：

- 结构化日志，默认不记录原始音频和完整识别文本。
- 脱敏诊断包。
- CPU、内存、队列长度、ASR 延迟监控。
- 资源上限和异常降级。
- 隐私说明与本地数据删除入口。

验证：

- 8 小时待机、2 小时持续听写压力测试。
- 待机时 CPU 目标均值 < 3%；监听时按基准机记录并设回归阈值。
- 内存稳定，无持续线性增长。
- 诊断包中不得出现原始音频、声纹向量、ChatGPT 内容或用户目录敏感信息。
- 一键删除数据后模型以外的用户数据不可恢复访问。

验收：没有 P0/P1 缺陷，所有核心功能测试通过，性能指标形成基准报告。

## 6. 功能验收矩阵

| 编号 | 功能 | 自动验证 | 人工跑通 | 通过标准 |
|---|---|---|---|---|
| F01 | 单实例启动 | E2E 启动两次 | 双击两次图标 | 只有一个进程组和窗口 |
| F02 | 首次引导 | 页面状态测试 | 完成四步配置 | 重启后不重复出现 |
| F03 | 页面导航 | UI 测试 | 逐页点击 | 五个页面内容正确、无丢状态 |
| F04 | 悬浮球 | 窗口 E2E | 拖动、展开、暂停 | 始终置顶、可记忆位置、不抢焦点 |
| F05 | 麦克风授权 | 权限状态测试 | 允许/拒绝各一次 | 状态与系统权限一致 |
| F06 | 设备选择 | mock 设备测试 | 切换两个输入设备 | 2 秒内切换且不中断应用 |
| F07 | 音频采集 | PCM fixture | 讲话观察音量 | 16 kHz mono，不卡顿不堆积 |
| F08 | 模型下载 | 中断/损坏测试 | 下载并删除一次 | 可恢复且哈希校验正确 |
| F09 | sidecar 管理 | 进程故障测试 | 人工结束进程 | 能重启并显示状态 |
| F10 | VAD 开始/结束 | 音频 fixture | 说话与静音交替 | 不截首字，静音不误触发 |
| F11 | 流式 partial | 事件序列测试 | 连续口述 | partial 可修正、不重复提交 |
| F12 | ASR final | 标准音频回归 | 中文/中英各 10 句 | 每段仅一个 final，顺序正确 |
| F13 | 自动标点 | 文本 fixture | 口述长句 | final 有合理标点，可关闭 |
| F14 | 声纹录制 | 质量规则测试 | 录制 5 段 | 不合格样本不可保存 |
| F15 | 声纹持久化 | 加密读写测试 | 重启后验证 | 可用且裸文件不可直接读 |
| F16 | 本人验证 | 本人测试集 | 用户讲话 | 安静环境通过率 ≥ 95% |
| F17 | 他人拒绝 | 非本人测试集 | 两名他人讲话 | 误通过率 ≤ 5% |
| F18 | 短音频处理 | 短音频 fixture | 单字、咳嗽 | 显示无法判断，不提交 |
| F19 | CDP 发现 | fake CDP 测试 | 启动 ChatGPT | 5 秒内显示已连接 |
| F20 | CDP 写入 | 字符串契约测试 | 焦点在其他应用讲话 | 不抢焦点且文本一致 |
| F21 | 草稿合并 | 幂等/合并测试 | 输入框预置文字 | 按保留/替换设置执行 |
| F22 | CDP 重连 | 断线 E2E | 重启 ChatGPT | 自动恢复，不重复文本 |
| F23 | 手动发送 | CDP 契约测试 | 点击发送 | 仅点击后发送一次 |
| F24 | 语音发送 | 命令解析测试 | 开启后说“发送” | 只在明确命令时发送一次 |
| F25 | 全局快捷键 | 主进程测试 | 在其他应用按快捷键 | 切换监听且不输入快捷键字符 |
| F26 | 设置持久化 | store 测试 | 修改后重启 | 全部恢复 |
| F27 | 开机启动 | 平台集成测试 | 登出再登录 | 后台启动且不弹主窗口 |
| F28 | 退出清理 | E2E 进程检查 | 从托盘退出 | 麦克风释放、无残留进程 |
| F29 | 日志脱敏 | 敏感词扫描 | 导出诊断包 | 无音频、声纹、对话正文 |
| F30 | 安装升级 | 安装矩阵 | 安装旧版再升级 | 配置保留、功能可用 |

## 7. 测试体系

### 每次提交

- TypeScript 类型检查。
- ESLint 与格式检查。
- React / 状态机 / CDP adapter 单元测试。
- C++ sidecar 单元测试与 CTest。
- IPC schema 兼容性测试。

### 每个合并请求

- Electron Playwright 冒烟测试。
- 页面截图回归。
- fake CDP 端到端写入。
- 固定音频集 VAD / ASR / speaker 回归。
- macOS 与 Windows CI 构建。

### 每个候选发布版

- 真实 ChatGPT 兼容性测试。
- 两端干净机器安装测试。
- 本人/非本人声纹测试。
- 2 小时持续听写与 8 小时待机。
- 权限、签名、升级、卸载和隐私检查。

## 8. 完成定义

一个功能只有同时满足以下条件才可标记完成：

1. 正常路径实现并可从 UI 使用。
2. 错误路径有明确提示且可恢复。
3. 有自动化测试，或明确说明为何只能人工验证。
4. macOS 与 Windows 对应测试均通过。
5. 验收步骤与结果有记录。
6. 不产生未授权的音频、声纹或识别文本持久化。
7. 文档包含用户操作方法和已知限制。

“代码已写”“本机能运行一次”均不等于完成。

## 9. 推荐执行顺序与周期

按一名熟悉 Electron、具备基础 C++ 能力的全职开发者估算：

- 技术 PoC：1–2 周。
- UI 与工程骨架：1 周。
- 音频、模型、VAD、ASR：2 周。
- 声纹与 CDP：2 周。
- 跨平台打包与稳定性：2–3 周。

可演示 MVP 约 6–8 周；达到可分发的测试版本约 9–12 周。若 macOS、Windows 并行开发，可缩短日历时间，但不能省略双平台验收。

## 10. 第一轮开发任务

第一轮只做风险闭环，不先铺完整产品功能：

1. 整理现有 Electron 原型，接入 React、TypeScript、Tailwind 与 shadcn/ui。
2. 创建 sidecar 最小工程并完成 Electron 启停和健康检查。
3. 跑通麦克风 → VAD → 流式 ASR。
4. 跑通声纹登记 → 本人通过 → 他人拒绝。
5. 跑通 CDP 发现 → 无焦点写入 → 断线重连。
6. 在 macOS 与 Windows 各制作一个最小安装包。
7. 输出四项 Spike 的测试报告，并据结果冻结生产架构。

第一轮完成后再进入完整 UI 与产品开发。
